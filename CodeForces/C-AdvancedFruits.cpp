#include<bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);cin.tie(0);
    string s1, s2;
    while(cin >> s1 >> s2){
        string mx, mn;
        mx = s1.length() >= s2.length() ? s1 : s2;
        mn = s1.length() < s2.length() ? s1 : s2;
        int p = 0;
        string ans;
        for( int i = 0; i < mx.length(); i++ ){
            
        }


    }
    return 0;
}